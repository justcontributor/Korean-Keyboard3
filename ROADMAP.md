  * 두벌식, 세벌식, 모바일 자판 기틀 완성
    * 특수 키를 제외한 한글 입력 레이아웃을 중심으로 구현
  * import 기능을 이용해 부가 기능 제공
    * k3lp local import 구현 필요
    * 한/영 변환 실제 레이어 지원
    * 모바일 기호 레이어 지원
      * 원화 기호 반영 필요
    * `ko-transforms.xml`과 `ko-transforms-bksp.xml`, `ko-variables.xml`의 import로 IME 구현
      * 현대 한글 IME를 모사하는 LDML 변환 및 변수 구문은 개발되었으나, import 환경 시험이 불가하고 구문이 방대해 기본으로는 포함하지 않음
      * 추가로 옛한글 등의 완성형 조합, 해제도 LDML 자체 지원
  * `touch` 환경의 길게 누르기 키 분석 후 더 세세히 지원
  * 천지인 레이아웃에 플릭 등도 지원
  * 천지인/나랏글 레이아웃의 중앙 등 variant 지원
  * 명명 관례
    * 파일의 이름은 `ko-(두벌식/세벌식/모바일)-(서브타입).xml`로 한다.
    * `info`의 `name`은 다음과 같이 정한다.
      * 두벌식/세벌식이면: `Korean (두벌식/세벌식) (서브타입)`
      * 모바일이면: `Korean (서브타입)`
      * 이때, 로마자 표기법 등을 이용해 영문 알파벳으로만 작성하며, `info` 태그 밑에 한국어 명칭을 주석으로 병기한다.
    * 대한민국의 로케일을 `ko`, 북한 로케일을 `ko-KP`로 한다.
    * `indicator` 속성은 `KO`로 정의한다.